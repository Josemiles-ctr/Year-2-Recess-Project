[project]
name = "year-2-recess"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1",
    "python-dotenv>=1.0",
]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
